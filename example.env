# Server port - The port on which the application will run
PORT=3000

# App package name - Your application's unique identifier in the AugmentOS ecosystem
PACKAGE_NAME=com.augmentos.reminders

# API key - Your AugmentOS developer API key (never commit the real key to repository)
API_KEY=your_api_key_here

# Public URL - The publicly accessible URL where your app is hosted
# For local development, you can use ngrok or a similar tool to create a tunnel
PUBLIC_URL=https://your-public-url.ngrok-free.app 